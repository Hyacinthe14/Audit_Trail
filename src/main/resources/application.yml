server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/
    username: postgres
    password: 
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
